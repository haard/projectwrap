# Example init.fish — runs inside the sandbox after project loads
# Customize this for your project, or delete if not needed.

# Environment variables
# set -gx KUBECONFIG ~/.kube/myproject/config
# set -gx DATABASE_URL postgres://localhost/myproject

# Virtual environment activation
# source .venv/bin/activate.fish

# Project-specific aliases
# alias run "python manage.py runserver"

# Tool version switching
# pyenv local 3.12.0
